/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        // 例: brand: { DEFAULT: '#0070f3', dark: '#0051a2' },
      },
      fontFamily: {
        // 例: sans: ['Inter', 'sans-serif'],
      },
      spacing: {
        // 例: '128': '32rem',
      },
    },
  },
  plugins: [
    // 例: require('@tailwindcss/typography'),
  ],
};
